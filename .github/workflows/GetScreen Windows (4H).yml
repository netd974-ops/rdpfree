name: GetScreen Windows (4H)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Downloading & Setting Up
        shell: powershell
        run: |
          # إنشاء متغير البيئة الذي تبحث عنه الأداة أثناء التثبيت
          [System.Environment]::SetEnvironmentVariable('netd974@gmail.com', 'jordanegaylord8@gmail.com', 'Machine')
          [System.Environment]::SetEnvironmentVariable('ghietriderghost@gmail.com', 'jordanegaylord8@gmail.com', 'Machine')
          
          # تحميل وتشغيل ملف التثبيت
          Invoke-WebRequest -Uri "https://dropbox.com" -OutFile "down.bat"
          Start-Process -FilePath "cmd.exe" -ArgumentList "/c down.bat" -Wait

      - name: Time Counter
        shell: cmd
        run: |
          echo # هادا هو المكتوب داخل الملف
          python -c "import time; print('Starting counter...'); time.sleep(14400)"

